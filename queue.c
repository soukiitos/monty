#include "monty.h"
/**
 * queue_top - Set the format of the data to a queue (FIFO)
 * @h: A pointer
 * @size: The size
 *
 * Return: 0
 */
void queue_top(stack_t **h, unsigned int size)
{
	(void)h;
	(void)size;
	val.flag_L = 1;
}
