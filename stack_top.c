#include "monty.h"
/**
 * stack_top - Set the format of the data to a stack (LIFO)
 * @h: A pointer
 * @size: The size
 *
 * Return: 0
 */
void stack_top(stack_t **h, unsigned int size)
{
	(void)h;
	(void)size;
	val.flag_L = 0;
}
